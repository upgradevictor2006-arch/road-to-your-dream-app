"""
Расширенный ИИ-агент - Личный менеджер
Обеспечивает навигацию, разбиение целей на этапы, мотивацию и советы
"""

import os
import httpx
import json
import logging
import re
from typing import List, Dict, Optional, Any
from datetime import datetime, date, timedelta
from dotenv import load_dotenv

load_dotenv()
logger = logging.getLogger(__name__)


class PersonalAIManager:
    """
    Расширенный ИИ-агент, работающий как личный менеджер
    Предоставляет: навигацию, разбиение целей на этапы, мотивацию, советы
    """
    
    @staticmethod
    def _fix_punctuation(text: str) -> str:
        """
        Исправляет пунктуацию в тексте: убирает запятые после точек
        """
        if not text:
            return text
        
        # Заменяем ".," на "."
        text = re.sub(r'\.\s*,', '. ', text)
        # Заменяем ". ," на "."
        text = re.sub(r'\.\s+,\s*', '. ', text)
        # Заменяем множественные точки с запятыми
        text = re.sub(r'\.\s*,\s*\.', '.', text)
        # Убираем лишние пробелы
        text = re.sub(r'\s+', ' ', text)
        # Убираем пробелы перед точками
        text = re.sub(r'\s+\.', '.', text)
        # Добавляем пробелы после точек (если их нет)
        text = re.sub(r'\.([^\s\.])', r'. \1', text)
        # Очищаем пробелы в начале и конце
        text = text.strip()
        
        return text
    
    @staticmethod
    def _remove_english_words(text: str) -> str:
        """
        Исправляет смешение языков в тексте (например, "Читайтеregularly" -> "Читайте регулярно")
        Заменяет только явно неправильные случаи смешения, не удаляет корректные английские термины
        """
        if not text:
            return text
        
        # Исправляем смешение языков: русское слово + английское слово без пробела
        # Например: "Читайтеregularly" -> "Читайте регулярно"
        text = re.sub(r'([а-яА-ЯёЁ]+)([a-zA-Z]+)', r'\1 \2', text)
        
        # Исправляем обратное: английское слово + русское слово без пробела
        # Например: "regularlyчитайте" -> "regularly читайте"
        text = re.sub(r'([a-zA-Z]+)([а-яА-ЯёЁ]+)', r'\1 \2', text)
        
        # Словарь замен только для явно неправильных случаев смешения
        # Заменяем только если слово стоит отдельно и явно является ошибкой
        replacements = {
            r'\bregularly\b(?=\s|$|[.,!?;:])': 'регулярно',  # Только если стоит отдельно
            r'\bdaily\b(?=\s|$|[.,!?;:])': 'ежедневно',
            r'\bweekly\b(?=\s|$|[.,!?;:])': 'еженедельно',
            r'\bmonthly\b(?=\s|$|[.,!?;:])': 'ежемесячно',
        }
        
        # Применяем замены только для явных ошибок
        for eng_word, rus_word in replacements.items():
            text = re.sub(eng_word, rus_word, text, flags=re.IGNORECASE)
        
        # Убираем двойные пробелы
        text = re.sub(r'\s+', ' ', text)
        text = text.strip()
        
        return text
    
    @staticmethod
    def _fix_natural_language(text: str) -> str:
        """
        Исправляет неестественные формулировки на естественный русский язык
        """
        if not text:
            return text
        
        # Исправляем "регистрировались в приложении N дней"
        text = re.sub(
            r'([Рр]егистрировались|[Рр]егистрировался|[Рр]егистрировалась)\s+в\s+приложении\s+(\d+)\s+дн(я|ей|ень)',
            r'С момента регистрации прошло \2 дн\3',
            text
        )
        
        # Исправляем другие неестественные формулировки
        # "пользуетесь приложением N дней" -> "пользуетесь приложением уже N дней"
        text = re.sub(
            r'([Пп]ользуетесь|[Пп]ользуется)\s+приложением\s+(\d+)\s+дн(я|ей|ень)(?![уа])',
            r'\1 приложением уже \2 дн\3',
            text
        )
        
        return text.strip()
    
    def __init__(self):
        """Инициализация личного менеджера"""
        # Проверяем доступные API ключи
        self.groq_key = os.getenv('GROQ_API_KEY')
        self.huggingface_key = os.getenv('HUGGINGFACE_API_KEY')
        self.cohere_key = os.getenv('COHERE_API_KEY')
        self.deepseek_key = os.getenv('DEEPSEEK_API_KEY')
        
        # Базовый системный промпт для личного менеджера - мудрец-философ
        self.base_system_prompt = (
            "Ты — мудрый философ и наставник в приложении для людей, стремящихся к достижению своих целей. "
            "Твоя роль — быть проводником и помощником на пути к любым разумным целям. "
            "Ты обладаешь глубоким пониманием человеческой природы, процессов достижения целей и философии самосовершенствования. "
            "Ты должен проверять цели на: "
            "1. ВЫПОЛНИМОСТЬ — можно ли это сделать в реальной жизни (не фантастика, не магия, не сверхспособности) "
            "2. ЗАКОННОСТЬ — не нарушает ли цель законы и этические нормы "
            "3. РЕАЛИСТИЧНОСТЬ — есть ли физическая возможность это сделать (не требует сверхчеловеческих способностей) "
            "ВАЖНО: Ты НЕ должен отсеивать цели только потому, что они кажутся 'несерьезными' или 'незначительными'. "
            "Если цель выполнима, законна и реалистична — она хорошая, независимо от того, насколько она 'серьезная'. "
            "Примеры ХОРОШИХ целей: 'Посмотреть все фильмы Джонни Деппа', 'Выучить 1000 слов на английском', 'Прочитать 50 книг за год', 'Научиться готовить пиццу', 'Пробежать марафон'. "
            "Примеры ПЛОХИХ целей: 'Стать супергероем', 'Научиться летать', 'Украсть миллион долларов', 'Стать бессмертным', 'Победить всех в драке'. "
            "Если цель невыполнима, незаконна или нереалистична — мягко укажи на проблему и помоги сформулировать выполнимую версию. "
            "Если цель выполнима и законна — принимай её и помогай разбить на этапы, даже если она кажется простой или развлекательной. "
            "Твой стиль общения: уважительный, глубокий, но не пафосный. Ты говоришь как мудрый наставник, который видел много путей. "
            "Используй метафоры пути, восхождения, мастерства, но делай это естественно и по делу. "
            "ВАЖНО: Отвечай на русском языке. Используй русский язык для всех текстов, советов и описаний. "
            "Если нужно использовать английский термин (название, аббревиатура, технический термин) - используй его правильно, отдельно от русского текста. "
            "НЕ смешивай языки в одном слове или фразе (например, 'Читайтеregularly' - это ошибка, правильно 'Читайте регулярно' или 'Read regularly'). "
            "Дай конкретные, применимые, глубокие советы на русском языке. "
        )
        
        # Статистика использования
        self.stats = {
            'groq_success': 0,
            'groq_failures': 0,
            'deepseek_success': 0,
            'deepseek_failures': 0,
            'cohere_success': 0,
            'cohere_failures': 0,
            'huggingface_success': 0,
            'huggingface_failures': 0,
            'fallback_used': 0,
            'total_requests': 0
        }
    
    async def _call_ai(self, system_prompt: str, user_prompt: str, max_tokens: int = 500) -> Optional[str]:
        """
        Вызов ИИ API с fallback стратегией
        Приоритет: DeepSeek > Groq > Cohere > HuggingFace
        
        Args:
            system_prompt: Системный промпт
            user_prompt: Пользовательский промпт
            max_tokens: Максимальное количество токенов
            
        Returns:
            Ответ от ИИ или None
        """
        self.stats['total_requests'] += 1
        
        # Проверяем доступные API ключи
        available_apis = []
        if self.deepseek_key:
            available_apis.append("DeepSeek")
        if self.groq_key:
            available_apis.append("Groq")
        if self.cohere_key:
            available_apis.append("Cohere")
        if self.huggingface_key:
            available_apis.append("HuggingFace")
        
        if not available_apis:
            logger.error("❌ Нет доступных API ключей! Проверьте .env файл.")
            self.stats['fallback_used'] += 1
            return None
        
        logger.info(f"🔄 Пробуем ИИ API (доступны: {', '.join(available_apis)})")
        
        # Пробуем DeepSeek (лучший для серьезных задач)
        if self.deepseek_key:
            logger.debug("Пробуем DeepSeek...")
            result = await self._try_deepseek(system_prompt, user_prompt, max_tokens)
            if result:
                return result
        
        # Пробуем Groq (быстрый и бесплатный)
        if self.groq_key:
            logger.debug("Пробуем Groq...")
            result = await self._try_groq(system_prompt, user_prompt, max_tokens)
            if result:
                return result
        
        # Пробуем Cohere (качественный API)
        if self.cohere_key:
            logger.debug("Пробуем Cohere...")
            result = await self._try_cohere(system_prompt, user_prompt, max_tokens)
            if result:
                return result
        
        # Пробуем HuggingFace (резервный)
        if self.huggingface_key:
            logger.debug("Пробуем HuggingFace...")
            result = await self._try_huggingface(system_prompt, user_prompt, max_tokens)
            if result:
                return result
        
        # Если ничего не сработало, используем fallback
        self.stats['fallback_used'] += 1
        logger.warning(f"⚠️ Все ИИ API ({', '.join(available_apis)}) недоступны, используем fallback ответ")
        return None
    
    async def _try_deepseek(self, system_prompt: str, user_prompt: str, max_tokens: int) -> Optional[str]:
        """Попытка использования DeepSeek API"""
        try:
            async with httpx.AsyncClient(timeout=30.0) as client:
                response = await client.post(
                    "https://api.deepseek.com/v1/chat/completions",
                    headers={
                        "Authorization": f"Bearer {self.deepseek_key}",
                        "Content-Type": "application/json"
                    },
                    json={
                        "model": "deepseek-chat",
                        "messages": [
                            {"role": "system", "content": system_prompt},
                            {"role": "user", "content": user_prompt}
                        ],
                        "max_tokens": max_tokens,
                        "temperature": 0.7
                    }
                )
                
                if response.status_code == 200:
                    result = response.json()
                    if 'choices' in result and len(result['choices']) > 0:
                        text = result['choices'][0]['message']['content']
                        if text:
                            self.stats['deepseek_success'] += 1
                            logger.info("✅ DeepSeek успешно обработал запрос")
                            return text.strip()
                    else:
                        logger.warning(f"DeepSeek вернул пустой ответ: {result}")
                else:
                    error_text = ""
                    try:
                        error_data = response.json()
                        error_text = str(error_data)
                    except:
                        error_text = response.text[:200]
                    self.stats['deepseek_failures'] += 1
                    logger.warning(f"❌ DeepSeek недоступен: статус {response.status_code}, ошибка: {error_text}")
        except Exception as e:
            self.stats['deepseek_failures'] += 1
            logger.error(f"❌ DeepSeek исключение: {type(e).__name__}: {str(e)}")
        return None
    
    async def _try_groq(self, system_prompt: str, user_prompt: str, max_tokens: int) -> Optional[str]:
        """Попытка использования Groq API"""
        try:
            async with httpx.AsyncClient(timeout=30.0) as client:
                response = await client.post(
                    "https://api.groq.com/openai/v1/chat/completions",
                    headers={
                        "Authorization": f"Bearer {self.groq_key}",
                        "Content-Type": "application/json"
                    },
                    json={
                        "model": "llama-3.1-8b-instant",  # Используем доступную модель
                        "messages": [
                            {"role": "system", "content": system_prompt},
                            {"role": "user", "content": user_prompt}
                        ],
                        "max_tokens": max_tokens,
                        "temperature": 0.7
                    }
                )
                
                if response.status_code == 200:
                    result = response.json()
                    if 'choices' in result and len(result['choices']) > 0:
                        text = result['choices'][0]['message']['content']
                        if text:
                            self.stats['groq_success'] += 1
                            logger.info("✅ Groq успешно обработал запрос")
                            return text.strip()
                    else:
                        logger.warning(f"Groq вернул пустой ответ: {result}")
                else:
                    error_text = ""
                    try:
                        error_data = response.json()
                        error_text = str(error_data)
                    except:
                        error_text = response.text[:200]
                    self.stats['groq_failures'] += 1
                    logger.warning(f"❌ Groq недоступен: статус {response.status_code}, ошибка: {error_text}")
        except Exception as e:
            self.stats['groq_failures'] += 1
            logger.error(f"❌ Groq исключение: {type(e).__name__}: {str(e)}")
        return None
    
    async def _try_cohere(self, system_prompt: str, user_prompt: str, max_tokens: int) -> Optional[str]:
        """Попытка использования Cohere API"""
        try:
            async with httpx.AsyncClient(timeout=30.0) as client:
                response = await client.post(
                    "https://api.cohere.ai/v1/chat",
                    headers={
                        "Authorization": f"Bearer {self.cohere_key}",
                        "Content-Type": "application/json"
                    },
                    json={
                        "model": "command",
                        "message": user_prompt,
                        "system": system_prompt,
                        "max_tokens": max_tokens,
                        "temperature": 0.7
                    }
                )
                
                if response.status_code == 200:
                    result = response.json()
                    if 'text' in result:
                        text = result['text']
                        if text:
                            self.stats['cohere_success'] += 1
                            logger.info("Cohere успешно обработал запрос")
                            return text.strip()
                
                self.stats['cohere_failures'] += 1
                logger.warning(f"Cohere недоступен: {response.status_code}")
        except Exception as e:
            self.stats['cohere_failures'] += 1
            logger.warning(f"Cohere ошибка: {e}")
        return None
    
    async def _try_huggingface(self, system_prompt: str, user_prompt: str, max_tokens: int) -> Optional[str]:
        """Попытка использования HuggingFace API"""
        try:
            # Используем модель для чата
            async with httpx.AsyncClient(timeout=30.0) as client:
                response = await client.post(
                    "https://api-inference.huggingface.co/models/meta-llama/Llama-2-7b-chat-hf",
                    headers={"Authorization": f"Bearer {self.huggingface_key}"},
                    json={
                        "inputs": f"{system_prompt}\n\nUser: {user_prompt}\n\nAssistant:",
                        "parameters": {
                            "max_new_tokens": max_tokens,
                            "temperature": 0.7
                        }
                    }
                )
                
                if response.status_code == 200:
                    result = response.json()
                    if isinstance(result, list) and len(result) > 0:
                        text = result[0].get('generated_text', '')
                        if text:
                            # Извлекаем только ответ ассистента
                            if 'Assistant:' in text:
                                text = text.split('Assistant:')[-1].strip()
                            self.stats['huggingface_success'] += 1
                            logger.info("HuggingFace успешно обработал запрос")
                            return text.strip()
                
                self.stats['huggingface_failures'] += 1
                logger.warning(f"HuggingFace недоступен: {response.status_code}")
        except Exception as e:
            self.stats['huggingface_failures'] += 1
            logger.warning(f"HuggingFace ошибка: {e}")
        return None
    
    async def break_goal_into_steps(self, goal_title: str, goal_description: str = "", goal_type: str = "goal") -> Dict[str, Any]:
        """
        Разбивает цель на конкретные этапы
        
        Args:
            goal_title: Название цели
            goal_description: Описание цели
            goal_type: Тип цели
            
        Returns:
            Словарь с этапами и рекомендациями
        """
        system_prompt = (
            f"{self.base_system_prompt}"
            "Твоя задача — разбить цель на конкретные, измеримые этапы. "
            "СНАЧАЛА проверь цель на выполнимость, законность и реалистичность. "
            "Если цель НЕВЫПОЛНИМА (требует магии, сверхспособностей, фантастики), НЕЗАКОННА (нарушает законы) или НЕРЕАЛИСТИЧНА (физически невозможно) — "
            "верни JSON с полем 'is_serious': false и 'feedback' с объяснением проблемы и как сделать цель выполнимой. "
            "Если цель выполнима, законна и реалистична — разбей ее на этапы, даже если она кажется простой или развлекательной. "
            "Каждый этап должен быть:"
            "- Конкретным и понятным (не абстрактным)"
            "- Измеримым (с четким критерием выполнения)"
            "- Выполнимым за 1-4 недели (в зависимости от масштаба цели)"
            "- С логической последовательностью (каждый этап готовит к следующему)"
            "- Значимым (каждый этап приближает к результату)"
            "Верни ответ ТОЛЬКО в формате JSON:"
            '{"is_serious": true/false, "feedback": "только если is_serious=false (объяснение проблемы)", "steps": [{"title": "Название этапа", "description": "Описание", "estimated_days": число, "priority": число_1_5}], "advice": "рекомендации для достижения цели"}'
            "ВАЖНО: Все тексты в JSON должны быть на русском языке. Не смешивай языки в одном слове или фразе. "
            "Если нужно использовать английский термин (название, аббревиатура) - используй его правильно, отдельно от русского текста."
        )
        
        user_prompt = (
            f"Цель: {goal_title}\n"
            f"Описание: {goal_description or 'Нет описания'}\n"
            f"Тип: {goal_type}\n\n"
            "Сначала проверь: выполнима ли цель, законна ли она, реалистична ли она. "
            "Если есть проблемы — укажи их. Если цель хорошая — разбей на конкретные этапы. "
            "Помни: простые и развлекательные цели тоже хорошие, если они выполнимы и законны. "
            "Верни ТОЛЬКО JSON без дополнительного текста. Не смешивай языки в одном слове или фразе."
        )
        
        response = await self._call_ai(system_prompt, user_prompt, max_tokens=800)
        
        if response:
            try:
                # Пытаемся извлечь JSON из ответа
                # ИИ может добавить пояснения, ищем JSON блок
                json_start = response.find('{')
                json_end = response.rfind('}') + 1
                if json_start >= 0 and json_end > json_start:
                    json_str = response[json_start:json_end]
                    result = json.loads(json_str)
                    
                    # Удаляем английские слова из всех текстовых полей
                    if 'feedback' in result and result['feedback']:
                        result['feedback'] = self._remove_english_words(result['feedback'])
                    if 'advice' in result and result['advice']:
                        result['advice'] = self._remove_english_words(result['advice'])
                    if 'steps' in result:
                        for step in result['steps']:
                            if 'title' in step and step['title']:
                                step['title'] = self._remove_english_words(step['title'])
                            if 'description' in step and step['description']:
                                step['description'] = self._remove_english_words(step['description'])
                    
                    return result
            except json.JSONDecodeError:
                logger.warning(f"Не удалось распарсить JSON: {response}")
        
        # Fallback: создаем базовые этапы
        return self._get_fallback_steps(goal_title, goal_description)
    
    def _get_fallback_steps(self, goal_title: str, goal_description: str) -> Dict[str, Any]:
        """Fallback этапы если ИИ недоступен"""
        steps = [
            {
                "title": f"Изучить основы {goal_title}",
                "description": "Найти ресурсы и изучить основные концепции",
                "estimated_days": 7,
                "priority": 3
            },
            {
                "title": f"Составить план действий для {goal_title}",
                "description": "Детально расписать шаги и сроки",
                "estimated_days": 3,
                "priority": 4
            },
            {
                "title": f"Начать практическую реализацию {goal_title}",
                "description": "Приступить к выполнению первого практического шага",
                "estimated_days": 14,
                "priority": 5
            },
            {
                "title": f"Оценить прогресс в {goal_title}",
                "description": "Проанализировать результаты и скорректировать план",
                "estimated_days": 2,
                "priority": 2
            }
        ]
        
        return {
            "is_serious": True,
            "steps": steps,
            "advice": f"Разбей большую цель '{goal_title}' на небольшие шаги. Начни с изучения основ, затем составь план и приступай к практике. Регулярно оценивай прогресс."
        }
    
    async def break_goal_into_periods(self, goal_title: str, goal_description: str, total_days: int, period_structure: List[Dict] = None) -> Dict[str, Any]:
        """
        Разбивает цель на подпериоды с учетом общей длительности
        
        Args:
            goal_title: Название цели
            goal_description: Описание цели
            total_days: Общее количество дней для достижения цели
            period_structure: Структура периодов (годы, месяцы, недели) для заполнения задачами
            
        Returns:
            Словарь с заполненными задачами для каждого периода
        """
        system_prompt = (
            f"{self.base_system_prompt}"
            "Твоя задача — помочь человеку разбить его цель на подпериоды, заполнив каждый период конкретными задачами. "
            "СНАЧАЛА проверь цель на выполнимость, законность и реалистичность. "
            "Если цель невыполнима, незаконна или нереалистична — укажи это в feedback. "
            "Если цель хорошая (выполнима, законна, реалистична) — для каждого периода предложи:"
            "- Улучшенное название периода (более конкретное и отражающее задачу этого периода, например 'Изучение основ' вместо '1-й месяц')"
            "- Конкретную задачу или фокус для этого периода"
            "- Описание того, что нужно сделать за этот период"
            "- Логическую связь с предыдущим и следующим периодом"
            "Задачи должны быть реалистичными, измеримыми и приближающими к цели. "
            "Названия периодов должны быть конкретными и отражать суть этапа (например: 'Подготовка и планирование', 'Основной этап обучения', 'Практика и закрепление'). "
            "Помни: простые и развлекательные цели тоже хорошие, если они выполнимы. "
            "Верни ответ ТОЛЬКО в формате JSON:"
            '{"is_serious": true/false, "feedback": "только если is_serious=false (объяснение проблемы)", "periods": [{"id": "id периода", "title": "улучшенное конкретное название периода", "task": "конкретная задача для этого периода", "description": "подробное описание что делать", "days": число_дней_если_нужно_изменить}], "advice": "рекомендации по реализации"}'
            "ВАЖНО: Все тексты в JSON должны быть на русском языке. Не смешивай языки в одном слове или фразе. "
            "Названия, задачи, описания — на русском. Если нужно использовать английский термин - используй его правильно, отдельно. "
            "Ты можешь изменять названия периодов (дней, недель, месяцев) на более конкретные и осмысленные. "
            "Если нужно изменить количество дней для периода - укажи это в поле 'days'."
        )
        
        # Формируем описание структуры периодов
        periods_desc = ""
        if period_structure:
            periods_desc = "\nСтруктура периодов:\n"
            for period in period_structure:
                period_type = period.get('type', 'period')
                period_title = period.get('title', 'Период')
                period_days = period.get('days', 0)
                periods_desc += f"- {period_title} ({period_days} дней, тип: {period_type})\n"
        
        user_prompt = (
            f"Цель: {goal_title}\n"
            f"Описание: {goal_description or 'Нет описания'}\n"
            f"Общий период: {total_days} дней\n"
            f"{periods_desc}\n"
            "Сначала проверь: выполнима ли цель, законна ли она, реалистична ли она. "
            "Если есть проблемы — укажи их. Если цель хорошая — заполни каждый период конкретной задачей. "
            "Задачи должны логично выстраиваться в путь к цели. "
            "Помни: простые и развлекательные цели тоже хорошие, если они выполнимы и законны. "
            "Верни ТОЛЬКО JSON без дополнительного текста. Не смешивай языки в одном слове или фразе."
        )
        
        response = await self._call_ai(system_prompt, user_prompt, max_tokens=1200)
        
        if response:
            try:
                json_start = response.find('{')
                json_end = response.rfind('}') + 1
                if json_start >= 0 and json_end > json_start:
                    json_str = response[json_start:json_end]
                    result = json.loads(json_str)
                    
                    # Удаляем английские слова из всех текстовых полей
                    if 'feedback' in result and result['feedback']:
                        result['feedback'] = self._remove_english_words(result['feedback'])
                    if 'advice' in result and result['advice']:
                        result['advice'] = self._remove_english_words(result['advice'])
                    
                    # Обрабатываем периоды
                    if 'periods' in result and result['periods']:
                        for period in result['periods']:
                            # Убеждаемся, что все поля заполнены
                            if 'id' not in period:
                                continue
                            if 'title' in period and period['title']:
                                period['title'] = self._remove_english_words(period['title'])
                            if 'task' in period and period['task']:
                                period['task'] = self._remove_english_words(period['task'])
                            elif 'description' in period and period['description']:
                                period['task'] = self._remove_english_words(period['description'])
                            if 'description' in period and period['description']:
                                period['description'] = self._remove_english_words(period['description'])
                    
                    return result
            except json.JSONDecodeError:
                logger.warning(f"Не удалось распарсить JSON: {response}")
        
        # Fallback: возвращаем структуру без изменений
        if period_structure:
            return {
                "is_serious": True,
                "periods": [
                    {
                        "id": p.get('id', ''),
                        "title": p.get('title', 'Период'),
                        "task": f"Работа над целью '{goal_title}'",
                        "description": "Продолжайте движение к цели согласно плану"
                    }
                    for p in period_structure
                ],
                "advice": "Разбейте каждый период на конкретные задачи и регулярно отслеживайте прогресс."
            }
        
        return {
            "is_serious": True,
            "periods": [],
            "advice": "Определите конкретные задачи для каждого периода вашего пути к цели."
        }
    
    async def get_navigation_advice(self, user_data: Dict[str, Any]) -> Dict[str, Any]:
        """
        Получает советы по навигации: что делать дальше
        
        Args:
            user_data: Данные пользователя (цели, карты, действия)
            
        Returns:
            Словарь с рекомендациями по навигации
        """
        # Анализируем данные пользователя
        active_goals = [g for g in user_data.get('goals', []) if not g.get('is_completed', False)]
        active_cards = [c for c in user_data.get('cards', []) if c.get('status') == 'active']
        recent_actions = user_data.get('daily_actions', [])[:7]  # Последние 7 дней
        
        # Если у пользователя нет активных целей - сразу возвращаем специальный ответ
        if len(active_goals) == 0:
            return {
                "next_actions": [{
                    "title": "🎯 Поставь свою первую цель",
                    "description": "Начни свой путь к мечте! Перейди на вкладку 'Карта' и создай свою первую цель. Без цели невозможно двигаться вперед.",
                    "priority": 5
                }],
                "focus": "Сначала нужно определить направление! Создай свою первую цель, чтобы понять, куда двигаться.",
                "warnings": ["У тебя еще нет активных целей. Без цели невозможно получить персональные рекомендации."],
                "no_goals": True  # Флаг для фронтенда
            }
        
        system_prompt = (
            f"{self.base_system_prompt}"
            "Ты навигатор на пути к мечте. Проанализируй ситуацию пользователя и дай конкретные рекомендации:"
            "- Что делать прямо сейчас (1-2 конкретных действия)"
            "- На чем сосредоточиться в ближайшие дни"
            "- Какие препятствия могут возникнуть и как их преодолеть"
            "ВАЖНО: Используй правильную пунктуацию! После точки ставится точка и пробел. Никогда не ставь запятую после точки."
            "Верни ответ в формате JSON:"
            '{"next_actions": [{"title": "Действие", "description": "Описание", "priority": число}], "focus": "на чем сосредоточиться", "warnings": ["предупреждения"]}'
        )
        
        user_prompt = (
            f"Активные цели: {len(active_goals)}\n"
            f"Активные карты: {len(active_cards)}\n"
            f"Действий за последние 7 дней: {len(recent_actions)}\n"
            f"Топ-3 активных цели: {', '.join([g.get('description', g.get('title', 'Цель'))[:50] for g in active_goals[:3]])}\n\n"
            "Что пользователю делать дальше? Верни ТОЛЬКО JSON без дополнительного текста."
        )
        
        response = await self._call_ai(system_prompt, user_prompt, max_tokens=600)
        
        if response:
            try:
                json_start = response.find('{')
                json_end = response.rfind('}') + 1
                if json_start >= 0 and json_end > json_start:
                    json_str = response[json_start:json_end]
                    result = json.loads(json_str)
                    
                    # Исправляем пунктуацию и естественность языка в текстовых полях
                    if 'focus' in result and result['focus']:
                        result['focus'] = self._fix_punctuation(result['focus'])
                        result['focus'] = self._fix_natural_language(result['focus'])
                    if 'next_actions' in result:
                        for action in result['next_actions']:
                            if 'description' in action and action['description']:
                                action['description'] = self._fix_punctuation(action['description'])
                                action['description'] = self._fix_natural_language(action['description'])
                    if 'warnings' in result:
                        result['warnings'] = [
                            self._fix_natural_language(self._fix_punctuation(w)) if w else w 
                            for w in result['warnings']
                        ]
                    
                    return result
            except json.JSONDecodeError:
                logger.warning(f"Не удалось распарсить JSON: {response}")
        
        # Fallback рекомендации
        return self._get_fallback_navigation(active_goals, active_cards, recent_actions)
    
    def _get_fallback_navigation(self, active_goals: List, active_cards: List, recent_actions: List) -> Dict[str, Any]:
        """Fallback навигация"""
        next_actions = []
        
        if len(active_goals) == 0:
            next_actions.append({
                "title": "Поставь первую цель",
                "description": "Начни свой путь к мечте с постановки первой цели",
                "priority": 5
            })
        elif len(recent_actions) < 3:
            next_actions.append({
                "title": "Выполни ежедневное действие",
                "description": "Сделай хотя бы один маленький шаг к своей цели сегодня",
                "priority": 5
            })
        else:
            next_actions.append({
                "title": "Продолжай выполнять ежедневные действия",
                "description": "Твоя серия действий помогает двигаться к цели",
                "priority": 4
            })
        
        if len(active_cards) > 5:
            next_actions.append({
                "title": "Пересмотри приоритеты",
                "description": "У тебя много активных задач. Сосредоточься на самых важных",
                "priority": 3
            })
        
        return {
            "next_actions": next_actions[:3],
            "focus": "Сосредоточься на выполнении ежедневных действий и двигайся к своим целям маленькими шагами",
            "warnings": []
        }
    
    async def get_personal_advice(self, question: str, context: Dict[str, Any] = None) -> Dict[str, Any]:
        """
        Получает персональный совет по вопросу пользователя
        
        Args:
            question: Вопрос или запрос пользователя
            context: Контекст (цели, прогресс и т.д.)
            
        Returns:
            Словарь с советом и рекомендациями
        """
        context_str = ""
        if context:
            active_goals = [g for g in context.get('goals', []) if not g.get('is_completed', False)]
            context_str = f"У пользователя {len(active_goals)} активных целей. "
            if active_goals:
                context_str += f"Основные цели: {', '.join([g.get('description', g.get('title', ''))[:50] for g in active_goals[:3]])}"
        
        system_prompt = (
            f"{self.base_system_prompt}"
            "Ты мудрый философ и наставник. Дай глубокий, практичный, философски обоснованный совет на основе вопроса человека. "
            "Твой ответ должен быть глубоким, но практичным. Используй философские принципы, но всегда давай конкретные шаги. "
            "Относись к любым разумным вопросам и целям с уважением — даже если они кажутся простыми или развлекательными. "
            "Твой стиль: мудрый наставник, который видел много путей и знает цену настоящим усилиям. "
            "ВАЖНО: Используй правильную пунктуацию! После точки ставится точка и пробел. Никогда не ставь запятую после точки. "
            "Верни ответ в формате JSON:"
            '{"advice": "глубокий философский совет с практическими рекомендациями", "steps": ["конкретный шаг 1", "конкретный шаг 2"], "motivation": "вдохновляющая фраза о пути к цели"}'
            "ВАЖНО: Все тексты в JSON должны быть на русском языке. Не смешивай языки в одном слове или фразе. "
            "Если нужно использовать английский термин (название, аббревиатура) - используй его правильно, отдельно от русского текста."
        )
        
        user_prompt = f"Вопрос пользователя: {question}\n\nКонтекст: {context_str or 'Нет дополнительного контекста'}\n\nДай глубокий, мудрый совет на русском языке. Верни ТОЛЬКО JSON. Не смешивай языки в одном слове или фразе."
        
        response = await self._call_ai(system_prompt, user_prompt, max_tokens=500)
        
        if response:
            try:
                json_start = response.find('{')
                json_end = response.rfind('}') + 1
                if json_start >= 0 and json_end > json_start:
                    json_str = response[json_start:json_end]
                    result = json.loads(json_str)
                    
                    # Удаляем английские слова, исправляем пунктуацию и естественность языка в текстовых полях
                    if 'advice' in result and result['advice']:
                        result['advice'] = self._remove_english_words(result['advice'])
                        result['advice'] = self._fix_punctuation(result['advice'])
                        result['advice'] = self._fix_natural_language(result['advice'])
                    if 'motivation' in result and result['motivation']:
                        result['motivation'] = self._remove_english_words(result['motivation'])
                        result['motivation'] = self._fix_punctuation(result['motivation'])
                        result['motivation'] = self._fix_natural_language(result['motivation'])
                    if 'steps' in result:
                        result['steps'] = [
                            self._fix_natural_language(self._fix_punctuation(self._remove_english_words(step))) if step else step 
                            for step in result['steps']
                        ]
                    
                    logger.info(f"✅ ИИ успешно сгенерировал совет для вопроса: {question[:50]}")
                    return result
            except json.JSONDecodeError as e:
                logger.warning(f"Не удалось распарсить JSON: {response[:200]}")
                logger.warning(f"Ошибка парсинга: {e}")
                # Пытаемся использовать ответ как есть, если он не в JSON
                if response.strip():
                    return {
                        "advice": response.strip(),
                        "steps": [],
                        "motivation": ""
                    }
        else:
            logger.warning(f"⚠️ ИИ API не вернул ответ для вопроса: {question[:50]}. Используется fallback.")
            # Проверяем, есть ли вообще доступные API ключи
            available_apis = []
            if self.deepseek_key:
                available_apis.append("DeepSeek")
            if self.groq_key:
                available_apis.append("Groq")
            if self.cohere_key:
                available_apis.append("Cohere")
            if self.huggingface_key:
                available_apis.append("HuggingFace")
            
            if not available_apis:
                logger.error("❌ Нет доступных API ключей! Убедитесь, что хотя бы один ключ настроен в .env")
        
        # Fallback совет
        logger.info("Используется fallback ответ для чата")
        return {
            "advice": "Продолжай двигаться к своей цели маленькими шагами каждый день. Постоянство важнее скорости.",
            "steps": [
                "Определи один конкретный шаг",
                "Выполни его сегодня",
                "Повторяй ежедневно"
            ],
            "motivation": "Каждое маленькое действие приближает тебя к большой мечте!"
        }
    
    async def get_motivation(self, context: Dict[str, Any] = None) -> str:
        """
        Получает мотивационное сообщение
        
        Args:
            context: Контекст (прогресс, достижения и т.д.)
            
        Returns:
            Мотивационное сообщение
        """
        context_str = ""
        if context:
            completed_goals = len([g for g in context.get('goals', []) if g.get('is_completed', False)])
            active_goals = len([g for g in context.get('goals', []) if not g.get('is_completed', False)])
            recent_actions = len(context.get('daily_actions', []))
            context_str = f"У пользователя {completed_goals} выполненных целей, {active_goals} активных целей, {recent_actions} выполненных действий."
        
        system_prompt = (
            f"{self.base_system_prompt}"
            "Дай короткое (2-3 предложения) глубокое, вдохновляющее мотивационное сообщение, "
            "используя философские метафоры пути, мастерства, восхождения. "
            "Говори о ценности усилий, постоянства, движения вперед. "
            "Мотивация должна быть сильной, но не пафосной — для тех, кто готов действовать. "
            "ВАЖНО: Используй правильную пунктуацию! После точки ставится точка и пробел. Никогда не ставь запятую после точки. "
            "ВАЖНО: Отвечай на русском языке. Не смешивай языки в одном слове или фразе. "
            "Если нужно использовать английский термин (название, аббревиатура) - используй его правильно, отдельно от русского текста."
        )
        
        user_prompt = f"Контекст: {context_str or 'Человек на пути к своей цели'}\n\nДай глубокую, вдохновляющую мотивацию на русском языке. Только текст на русском, без JSON. Не смешивай языки в одном слове или фразе."
        
        response = await self._call_ai(system_prompt, user_prompt, max_tokens=200)
        
        if response:
            text = self._remove_english_words(response.strip())
            text = self._fix_punctuation(text)
            return self._fix_natural_language(text)
        
        # Fallback мотивация
        fallback_messages = [
            "Каждый шаг приближает тебя к мечте! Ты как путешественник, который не останавливается перед трудностями. Вперед!",
            "Твой путь к цели продолжается! Помни: великие путешествия состоят из маленьких шагов. Продолжай!",
            "Ты на правильном пути! Как опытный навигатор, ты знаешь куда идешь. Не останавливайся!"
        ]
        import random
        return random.choice(fallback_messages)
    
    async def analyze_progress(self, user_data: Dict[str, Any]) -> Dict[str, Any]:
        """
        Анализирует прогресс пользователя на основе данных из базы данных
        
        Args:
            user_data: Данные пользователя из БД (goals, daily_actions, cards, user)
            
        Returns:
            Словарь с детальным анализом и рекомендациями
        """
        # Анализируем данные из БД
        all_goals = user_data.get('goals', [])
        completed_goals = [g for g in all_goals if g.get('is_completed', False)]
        active_goals = [g for g in all_goals if not g.get('is_completed', False)]
        
        # Обрабатываем действия из БД
        actions = user_data.get('daily_actions', [])
        all_cards = user_data.get('cards', [])
        active_cards = [c for c in all_cards if c.get('status') == 'active']
        completed_cards = [c for c in all_cards if c.get('status') == 'completed']
        
        # Получаем дату регистрации пользователя
        user = user_data.get('user', {})
        user_created = None
        if user.get('created_at'):
            try:
                if isinstance(user['created_at'], str):
                    user_created = datetime.fromisoformat(user['created_at'].replace('Z', '+00:00')).date()
                else:
                    user_created = user['created_at'].date() if hasattr(user['created_at'], 'date') else date.today()
            except:
                user_created = date.today()
        else:
            user_created = date.today()
        
        # Правильно вычисляем streak: последовательные дни от сегодня назад
        streak = 0
        today = date.today()
        
        if actions:
            # Преобразуем все даты действий в объекты date и сортируем по убыванию
            action_dates = []
            for action in actions:
                action_date = action.get('action_date')
                if isinstance(action_date, str):
                    try:
                        action_dates.append(datetime.fromisoformat(action_date).date())
                    except:
                        try:
                            action_dates.append(datetime.strptime(action_date, '%Y-%m-%d').date())
                        except:
                            pass
                elif hasattr(action_date, 'date'):
                    action_dates.append(action_date.date() if hasattr(action_date, 'date') else action_date)
                else:
                    action_dates.append(action_date)
            
            # Убираем дубликаты и сортируем
            action_dates = sorted(set(action_dates), reverse=True)
            
            # Вычисляем streak: идем от сегодня назад, проверяя последовательность
            check_date = today
            for action_date in action_dates:
                if action_date == check_date:
                    streak += 1
                    check_date = check_date - timedelta(days=1)
                elif action_date < check_date:
                    break
        
        # Дополнительная статистика из БД
        days_since_start = (today - user_created).days if user_created else 0
        total_actions = len(actions)
        avg_actions_per_week = (total_actions / max(1, days_since_start / 7)) if days_since_start > 0 else 0
        
        # Процент выполнения целей
        goal_completion_rate = (len(completed_goals) / len(all_goals) * 100) if len(all_goals) > 0 else 0
        
        # Статистика по картам
        high_priority_cards = len([c for c in active_cards if c.get('priority', 1) >= 4])
        
        # Формируем детальную статистику для ИИ
        stats_summary = (
            f"📊 Статистика пользователя:\n"
            f"- С момента регистрации прошло {days_since_start} дней\n"
            f"- Всего целей: {len(all_goals)} (выполнено: {len(completed_goals)}, активно: {len(active_goals)})\n"
            f"- Процент выполнения целей: {goal_completion_rate:.1f}%\n"
            f"- Всего ежедневных действий: {total_actions}\n"
            f"- Текущая серия дней (streak): {streak}\n"
            f"- Средняя активность: {avg_actions_per_week:.1f} действий в неделю\n"
            f"- Всего карт: {len(all_cards)} (активно: {len(active_cards)}, выполнено: {len(completed_cards)})\n"
            f"- Высокоприоритетных активных карт: {high_priority_cards}\n"
        )
        
        system_prompt = (
            f"{self.base_system_prompt}"
            "Ты мудрый аналитик прогресса. Проанализируй статистику человека из базы данных и дай глубокий, философски обоснованный анализ его пути. "
            "Учти: серия дней (streak) - это последовательные дни с момента последнего действия до сегодня. "
            "Твой анализ должен быть глубоким, но практичным. Говори о реальных достижениях, реальных проблемах, реальных путях улучшения. "
            "Относись с уважением к любым целям человека — даже если они кажутся простыми или развлекательными. "
            "Используй философский подход: путь к цели — это не спринт, а марафон. Анализируй не только цифры, но и качество пути. "
            "ВАЖНО: Используй правильную пунктуацию! После точки ставится точка, затем пробел. Никогда не ставь запятую после точки. "
            "Разделяй предложения точками. Пример правильного формата: 'Текст первое предложение. Текст второе предложение.' "
            "ВАЖНО: Используй естественный русский язык! Не говори 'регистрировались в приложении N дней'. "
            "Вместо этого говори: 'Прошло N дней с момента регистрации' или 'Вы пользуетесь приложением уже N дней' или 'С момента регистрации прошло N дней'. "
            "Верни в формате JSON:"
            '{"strength": "сильные стороны (глубокий анализ реальных достижений, разделенные точками, используй естественный русский язык)", "weaknesses": "слабые места и области для роста (честный анализ, предложения разделены точками)", "recommendations": ["конкретная рекомендация 1", "конкретная рекомендация 2", "конкретная рекомендация 3"], "score": число_от_0_до_100}'
            "ВАЖНО: Все тексты в JSON должны быть на русском языке. Не смешивай языки в одном слове или фразе. "
            "Если нужно использовать английский термин (название, аббревиатура) - используй его правильно, отдельно от русского текста."
        )
        
        user_prompt = (
            f"{stats_summary}\n"
            f"Детали:\n"
            f"- Активные цели: {', '.join([g.get('description', g.get('goal_type', 'Цель'))[:30] for g in active_goals[:3]]) if active_goals else 'Нет активных целей'}\n"
            f"- Выполненные цели: {', '.join([g.get('description', g.get('goal_type', 'Цель'))[:30] for g in completed_goals[:3]]) if completed_goals else 'Нет выполненных целей'}\n"
            f"- Последние действия: {min(7, len(actions))} дней назад\n\n"
            f"Дай глубокий, философски обоснованный анализ прогресса человека на основе этих реальных данных. "
            f"Будь честным и глубоким. Верни ТОЛЬКО JSON. Не смешивай языки в одном слове или фразе."
        )
        
        response = await self._call_ai(system_prompt, user_prompt, max_tokens=700)
        
        if response:
            try:
                json_start = response.find('{')
                json_end = response.rfind('}') + 1
                if json_start >= 0 and json_end > json_start:
                    json_str = response[json_start:json_end]
                    result = json.loads(json_str)
                    
                    # Удаляем английские слова, исправляем пунктуацию и естественность языка в текстовых полях
                    if 'strength' in result and result['strength']:
                        result['strength'] = self._remove_english_words(result['strength'])
                        result['strength'] = self._fix_punctuation(result['strength'])
                        result['strength'] = self._fix_natural_language(result['strength'])
                    if 'weaknesses' in result and result['weaknesses']:
                        result['weaknesses'] = self._remove_english_words(result['weaknesses'])
                        result['weaknesses'] = self._fix_punctuation(result['weaknesses'])
                        result['weaknesses'] = self._fix_natural_language(result['weaknesses'])
                    if 'recommendations' in result:
                        result['recommendations'] = [
                            self._fix_natural_language(self._fix_punctuation(self._remove_english_words(rec))) if rec else rec
                            for rec in result['recommendations']
                        ]
                    
                    # Добавляем вычисленные значения из БД
                    result['streak'] = streak
                    result['days_since_start'] = days_since_start
                    result['total_actions'] = total_actions
                    result['avg_actions_per_week'] = round(avg_actions_per_week, 1)
                    result['goal_completion_rate'] = round(goal_completion_rate, 1)
                    return result
            except json.JSONDecodeError:
                logger.warning(f"Не удалось распарсить JSON: {response}")
        
        # Fallback анализ на основе данных из БД
        score = min(100, (
            len(completed_goals) * 15 +  # 15 баллов за каждую выполненную цель
            streak * 3 +  # 3 балла за каждый день серии
            min(30, total_actions // 2) +  # до 30 баллов за активность
            min(20, goal_completion_rate / 5)  # до 20 баллов за процент выполнения
        ))
        
        # Формируем детальные рекомендации на основе реальных данных
        recommendations = []
        if len(active_goals) == 0 and len(completed_goals) == 0:
            recommendations.append("Создай свою первую цель для начала пути к мечте")
        elif len(active_goals) == 0:
            recommendations.append("Создай новую цель - у тебя нет активных целей")
        
        if streak == 0:
            recommendations.append("Начни ежедневную серию - выполни действие сегодня")
        elif streak < 7:
            recommendations.append(f"Поддерживай свою серию из {streak} дней - не пропускай дни")
        
        if goal_completion_rate < 50 and len(all_goals) > 0:
            recommendations.append("Сфокусируйся на выполнении текущих целей")
        
        if high_priority_cards > 5:
            recommendations.append("У тебя много высокоприоритетных задач - расставь приоритеты")
        
        if not recommendations:
            recommendations = [
                "Продолжай в том же духе",
                "Регулярно анализируй свой прогресс",
                "Ставь новые цели после выполнения текущих"
            ]
        
        return {
            "strength": f"У тебя {len(completed_goals)} выполненных целей, серия из {streak} дней и {total_actions} выполненных действий" if streak > 0 and total_actions > 0 else f"У тебя {len(completed_goals)} выполненных целей и {total_actions} выполненных действий",
            "weaknesses": "Продолжай выполнять ежедневные действия для поддержания серии" if streak < 7 else "Отличная работа! Продолжай в том же духе",
            "recommendations": recommendations[:3],
            "score": int(score),
            "streak": streak,
            "days_since_start": days_since_start,
            "total_actions": total_actions,
            "avg_actions_per_week": round(avg_actions_per_week, 1),
            "goal_completion_rate": round(goal_completion_rate, 1)
        }
    
    def get_stats(self) -> Dict[str, int]:
        """Возвращает статистику использования"""
        return self.stats.copy()
    
    def reset_stats(self):
        """Сбрасывает статистику"""
        self.stats = {
            'groq_success': 0,
            'groq_failures': 0,
            'fallback_used': 0,
            'total_requests': 0
        }

